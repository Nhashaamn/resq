import 'package:flutter/material.dart';

class DesasterLocation extends StatelessWidget {
  const DesasterLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}